import { SortReposPipe } from './sort-repos.pipe';

describe('SortReposPipe', () => {
  it('create an instance', () => {
    const pipe = new SortReposPipe();
    expect(pipe).toBeTruthy();
  });
});
